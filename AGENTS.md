# wechat-notebank Agent 约定

## Agent skills

### Issue tracker

本项目使用 GitHub Issues 管理规格、需求和 Agent tickets。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用 `needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

本项目使用单上下文领域文档：根目录 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。
